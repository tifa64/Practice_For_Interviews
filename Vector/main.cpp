#include <iostream>
#include "Vector.h"
using namespace std;

int main()
{
    Vector v;
    /*int n;
    cin>>n;
    v.set_capacity(n);
    v.push(60);
    v.push(45);
    v.push(70);
    v.push(12);
    v.push(60);

    v.insrt(5,2);
    v.insrt(10,2);
    v.prepend(23);
    v.delet(2);
    v.removee(60);
    for(int i = 0 ; i < v.get_size() ; i++)
        cout<<v.att(i)<<' ';
    */
    return 0;
}
